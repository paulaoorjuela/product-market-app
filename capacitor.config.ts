import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paula.productmarketapp',
  appName: 'product-market-app',
  webDir: 'www'
};

export default config;
